const styles = theme => ({
  root: {
  },
  card: {
    marginBottom: theme.spacing(1),
  },
  actions: {
    justifyContent: "center"
  },
  edition: {
    textAlign: "right"
  },
  timer: {
    paddingTop: theme.spacing(1),
  }
});
export default styles;
