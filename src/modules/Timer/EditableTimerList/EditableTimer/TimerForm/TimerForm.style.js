const styles = theme => ({
  form: {
    width: '100%',
    marginTop: theme.spacing(1),
  },
  actions: {
    justifyContent: "center"
  },
  card: {
    marginBottom: theme.spacing(1),
  },
});
export default styles;
